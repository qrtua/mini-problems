# Phi-3.5-mini-instruct — Microsoft's compact model, punches above its weight
# VRAM: ~8GB | Fits: 1x 4090 very easily
# Good at: structured output, reasoning for its size
# Useful as: quick iteration / baseline comparison
# License: MIT
name: phi3.5-mini
model_path: microsoft/Phi-3.5-mini-instruct
vllm_args:
  tensor_parallel_size: 1
  max_model_len: 4096
  gpu_memory_utilization: 0.90
  trust_remote_code: true
